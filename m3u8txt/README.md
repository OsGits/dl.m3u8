# M3U8 链接集合

本目录提供了一些可用的 M3U8 链接资源，会不定期更新维护。

## 使用方法

如需使用这些链接，可直接在配置中修改为对应的完整 URL，格式如下：

```
https://raw.githubusercontent.com/OsGits/dl.m3u8/main/m3u8txt/[txt文件名].txt
```

其中 `[txt文件名]` 替换为当前目录中实际存在的文件名即可。

当前TXT文件类容说明：

- WYZY.txt  午夜小视频
- ****.txt  未来添加